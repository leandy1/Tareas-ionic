import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'conectividad-dispositivos',
  webDir: 'www'
};

export default config;
